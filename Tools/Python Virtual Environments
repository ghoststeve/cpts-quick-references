#Create a folder to house the virtual environment
mkdir python-env
cd python-env

#Create python virtual environment
python -m venv venv
python -m venv venv --prompt="py-env-1"

#Activate the environment
venv\scripts\activate #PowerShell
source venv/bin/activate #Linux

#Install packages
python -m pip install <package>
python -m pip llist
